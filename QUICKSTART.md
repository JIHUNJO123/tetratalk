# ⚡ 빠른 시작 가이드

한일 채팅 앱을 5분 안에 실행해보세요!

## 🎯 1단계: Firebase 설정 (5분)

### A. Firebase 프로젝트 생성

1. https://console.firebase.google.com/ 접속
2. "프로젝트 추가" 클릭
3. 프로젝트 이름: `korea-japan-chat` 입력
4. "프로젝트 만들기" 클릭

### B. Authentication 활성화

1. 좌측 메뉴 > 빌드 > **Authentication** 클릭
2. "시작하기" 클릭
3. "이메일/비밀번호" 선택 후 **활성화** 토글 켜기
4. "저장" 클릭

### C. Firestore 생성

1. 좌측 메뉴 > 빌드 > **Firestore Database** 클릭
2. "데이터베이스 만들기" 클릭
3. **"테스트 모드에서 시작"** 선택
4. 위치: `asia-northeast3 (Seoul)` 선택
5. "사용 설정" 클릭

### D. 웹 앱 등록

1. 프로젝트 개요 > ⚙️ (톱니바퀴) > 프로젝트 설정
2. 내 앱 섹션에서 **</> (웹)** 아이콘 클릭
3. 앱 닉네임: `korea-japan-chat` 입력
4. "앱 등록" 클릭
5. **Firebase SDK 구성 정보 복사** (아래 형식)

```javascript
const firebaseConfig = {
  apiKey: "AIza....",
  authDomain: "your-project.firebaseapp.com",
  projectId: "your-project-id",
  storageBucket: "your-project.appspot.com",
  messagingSenderId: "123456789",
  appId: "1:123456:web:abc123"
};
```

### E. 프로젝트에 Firebase 정보 입력

1. `src/services/firebase.js` 파일 열기
2. 위에서 복사한 정보로 교체:

```javascript
const firebaseConfig = {
  apiKey: "여기에_복사한_apiKey",
  authDomain: "여기에_복사한_authDomain",
  projectId: "여기에_복사한_projectId",
  storageBucket: "여기에_복사한_storageBucket",
  messagingSenderId: "여기에_복사한_messagingSenderId",
  appId: "여기에_복사한_appId"
};
```

3. 파일 저장 (Ctrl+S)

## 🚀 2단계: 앱 실행

앱이 이미 실행 중입니다! 터미널에 QR 코드가 표시되어 있습니다.

### 스마트폰으로 테스트하기 (가장 쉬움!)

1. **Expo Go** 앱 다운로드
   - 📱 [Android - Google Play](https://play.google.com/store/apps/details?id=host.exp.exponent)
   - 📱 [iOS - App Store](https://apps.apple.com/app/expo-go/id982107779)

2. Expo Go 앱 실행

3. 터미널의 **QR 코드 스캔**
   - Android: Expo Go에서 "Scan QR Code" 클릭
   - iOS: 카메라 앱으로 스캔

4. 앱이 자동으로 열립니다! 🎉

### 웹 브라우저로 테스트하기

터미널에서 **`w`** 키를 누르면 웹 브라우저에서 열립니다.

## 📱 3단계: 앱 사용해보기

### 회원가입 (2개 계정 필요)

**계정 1 - 한국어 사용자**
1. "계정이 없으신가요? 회원가입" 클릭
2. 정보 입력:
   - 닉네임: `테스트유저`
   - 이메일: `test1@test.com`
   - 비밀번호: `test123`
   - 언어: **한국어** 🇰🇷 선택
3. "회원가입" 클릭

**계정 2 - 일본어 사용자**
1. 로그아웃 (우측 상단)
2. 회원가입:
   - 닉네임: `テストユーザー`
   - 이메일: `test2@test.com`
   - 비밀번호: `test123`
   - 언어: **日本語** 🇯🇵 선택

### 채팅 시작

1. **"+ 새로운 채팅 시작"** 클릭
2. 상대방 선택 (다른 언어 사용자가 표시됨)
3. 메시지 입력 후 전송!

### 번역 테스트

**한국어 사용자가 보내면:**
```
안녕하세요
```

**일본어 사용자가 받으면:**
```
안녕하세요
번역: こんにちは
```

**일본어 사용자가 보내면:**
```
ありがとう
```

**한국어 사용자가 받으면:**
```
ありがとう
번역: 감사합니다
```

## 🎨 추가 설정 (선택사항)

### Firestore 보안 규칙 업데이트

프로덕션 배포 전에 보안 규칙을 강화하세요:

1. Firebase Console > Firestore Database > **규칙** 탭
2. 다음 규칙으로 교체:

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{userId} {
      allow read: if request.auth != null;
      allow write: if request.auth.uid == userId;
    }
    
    match /chatRooms/{chatRoomId} {
      allow read, write: if request.auth != null && 
        request.auth.uid in resource.data.participants;
      
      match /messages/{messageId} {
        allow read: if request.auth != null;
        allow create: if request.auth.uid == request.resource.data.senderId;
      }
    }
  }
}
```

3. "게시" 클릭

## 🔧 문제 해결

### "Firebase: Error (auth/...)" 오류

✅ **해결책:**
1. `src/services/firebase.js` 파일의 Firebase 구성 정보 재확인
2. Firebase Console에서 Authentication이 활성화되어 있는지 확인

### 번역이 안 됨

✅ **해결책:**
- 인터넷 연결 확인
- 잠시 기다리기 (무료 API는 약간 느릴 수 있음)

### 앱이 안 열림

✅ **해결책:**
```bash
# 캐시 삭제 후 재시작
npm start -- --clear
```

## 📚 더 자세한 정보

- **상세 가이드**: `USER_GUIDE.md` 참조
- **Firebase 설정**: `FIREBASE_SETUP.md` 참조
- **기술 문서**: `TECHNICAL_DOCS.md` 참조

## 🎉 완료!

이제 한일 채팅 앱을 사용할 수 있습니다!

**즐거운 개발 되세요!** 🚀🇰🇷🇯🇵

---

### 다음 단계

- [ ] 친구들과 테스트해보기
- [ ] UI 커스터마이징
- [ ] 추가 기능 구현
- [ ] 앱 스토어에 배포

**질문이 있으시면 README.md를 참조하세요!**
